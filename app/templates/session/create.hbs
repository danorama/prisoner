<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">New Participant</h3>
  </div>

  <form {{action 'newParticipant' on='submit'}}>
    <div class="panel-body">
      <div class="form-group has-feedback">
        <label class="control-label">Name</label>
        <span class="glyphicon glyphicon-user form-control-feedback"></span>
        {{input value=user.name type="text" class="form-control" placeholder="Pete Panther"}}
      </div>

      <div class="form-group has-feedback">
        <label>Email address</label>
        <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
        {{input value=user.email type="text" class="form-control" placeholder="pete123@mail.chapman.edu"}}
      </div>

      <div class="form-group has-feedback">
        <label>Session password</label>
        <span class="glyphicon glyphicon-lock form-control-feedback"></span>
        {{input value=user.password type="password" class="form-control"}}
      </div>
    </div>

    <div class="panel-footer">
      <button type="submit" class="btn btn-primary">Submit</button>
      {{link-to 'Reauthenticate' 'session.login' class='pull-right btn btn-link'}}
    </div>
  </form>
</div>
